"""Model package: a ~100M decoder whose attention backend is selectable
(`vanilla | gated | nsa`). The gated and NSA blocks are reused from the sibling
projects ../gated-attention and ../nsa-mini (see attention.py)."""
